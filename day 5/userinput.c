#include<stdio.h>
int main(){
    int n,arr[5];
    printf("enter the number of elements");
    scanf("%d ",&n);
    printf("print  5 elements");
    for( int i=0; i<n; i++){
        scanf("%d ",&arr[i]);
    }
        printf(" the array of elements arr...");//gjjhjuo909887786885

        for (int i=0; i<=n; i++ ){
            printf("%d ",arr[i]);
        }

    return 0;
}